ARG NGINX_IMAGE_TAG=1.27-alpine
FROM nginx:${NGINX_IMAGE_TAG}

COPY frontend-vue/dist/ /usr/share/nginx/html/
COPY deploy/nginx/frontend-vue-gateway.docker.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
