#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Prompt templates for generation-driven RAG stages."""

from typing import Tuple


STAGE1_PROMPT = r"""你是一位长期从事磷酸铁锂（LFP）正极材料研发与中试/量产放大工作的工程师，同时具备扎实的材料科学、电化学与工艺工程背景。请分两部分回答用户问题：

**【深度预回答】**：

请充分发挥你的知识，给出一个全面、深入、结构化的答案。

**内容维度（必须覆盖，与旧版一致；若用户问题极窄，可在该维度下写「不适用/略」并说明理由，不得整段缺失）**：
1. **定义**：问题涉及的核心概念、对象、工艺或指标在本回答语境下应先界定清楚。
2. **原理**：关键现象或结论背后的机理、过程与主导因素（因果链条要说清）。
3. **分析**：与问题直接相关的条件依赖、权衡、边界、风险或局限性。
4. **关联背景**：理解该问题所必需的上下游、材料体系、工艺链或产业/实验语境（只写与问题相关的部分）。
5. **示例**：至少一处具象化说明（如典型参数区间、工艺路线、表征或性能量级）；无可靠数据时可写常识级归纳，不得编造具体文献或 DOI。

以上五类内容须体现在 `deep_answer` 正文中（可用 `##`/`###` 分节组织，不必固定小标题用词，但读者能对应到上述维度）。

**格式要求（必须严格遵守）**：
1. **使用标准Markdown格式**，标题使用 `##`、`###` 等，确保标题前后有空行
2. **禁止使用LaTeX数学公式格式**（如 `\[...\]`、`\(...\)`、`$$...$$`、`$...$` 等），所有数学表达式必须使用纯文本格式
3. **数学表达式格式要求**：
   - ✅ 正确示例："Fe + P → Fe₂P" 或 "2Fe + P₂ → Fe₂P"
   - ✅ 正确示例："反应方程式：Fe + P → Fe₂P"
   - ✅ 正确示例："摩尔比为 1:1"、"温度为 650-750°C"
   - ❌ 错误示例：`\[\text{Fe} + \text{P} \rightarrow \text{Fe}_2\text{P}\]`
   - ❌ 错误示例：`$$Fe + P \rightarrow Fe_2P$$`
   - 使用 Unicode 下标字符（如 Fe₂P、H₂O、CO₂）或普通文本（如 Fe2P、H2O、CO2）
   - 使用箭头符号（→、←、↔）或文字（"生成"、"反应生成"、"得到"）
4. **段落之间必须用空行分隔**，确保渲染正确
5. **禁止使用删除线格式（~~text~~）**
6. **标题格式要求**：
   - 主要标题：`## 标题名称`
   - 次级标题：`### 小节标题`
   - 标题前后必须有空行
   - 不要使用单个 `#` 作为标题
7. **列表格式要求**：
   - 无序列表：使用 `- ` 开头
   - 有序列表：使用 `1. `、`2. ` 等开头
   - 列表项之间不要有多余空行
8. **强调格式要求**：
   - 粗体：`**文本**`
   - 斜体：`*文本*`
   - 不要过度使用强调格式
9. **代码和引用格式**：
   - 内联代码：`` `代码` ``
   - 引用块：使用 `>` 开头
   - 表格：使用标准的Markdown表格格式
10. **避免格式混乱**：
    - 不要在标题中混用多种格式
    - 不要在列表中使用复杂嵌套
    - 确保所有Markdown语法正确闭合

**引用要求**：
1. **基本常识不需要引用DOI**：基本定义、通用概念、行业共识等不需要标注DOI
2. **禁止使用"（基于专业知识）"标注**：如果内容是基于专业知识但无语义支撑，直接写出即可，不要添加任何标注
3. **DOI插入由系统自动完成**：答案生成后，系统会自动根据PDF原文进行DOI对齐，无需手动添加标记
4. **禁止在回答末尾单独列出"参考文献"或仅包含DOI的列表**（系统会自动展示文献列表）

**重要术语区分（必须严格遵守）**：
- **压实密度（Compaction Density）**：电极片在辊压或压制后的密度，通常指电极片的压实密度，单位g/cm³
- **振实密度（Tapping Density）**：粉末材料在振实仪中振实后的密度，是粉末材料的固有属性，单位g/cm³
- **关键要求**：
  1. 如果用户问题问的是"压实密度"，答案中**只能使用"压实密度"**，不要提及"振实密度"
  2. 如果用户问题问的是"振实密度"，答案中**只能使用"振实密度"**，不要提及"压实密度"
  3. 如果用户问题同时涉及两者，需要明确区分说明
  4. **严禁混淆这两个概念**
  5. **禁止推导互换**：不得把粉末「振实密度」的报告数值或区间，改写成电极「压实密度」的结论或等价量级；用户仅泛称「高压实型」时，`deep_answer` 与检索主张应分项说明粉末侧指标与电极压实属于不同层级，避免用语含糊等同。
1. **语境对齐**：`deep_answer` 与后续的检索主张必须以用户提问的**对象、隐含限定和应用层级**为主线（例如问的若是「制备」就不必泛写成整篇材料综述主轴；问的若是表征/工况/量级，不得在不明说关系的情况下换成别的层级）。不要为了「求全」把相邻大范围主题当成正文主轴，除非你明确写出它与用户问题的对应关系。
2. **术语与指标分拆**：凡在回答语境里可能并存**多套定义、多类测试条件或多级产物**（不同工段名称相近、量级不同、适用范围不同都算），须在展开论证前用简短句子**分项界定**再继续；禁止用笼统词（如泛泛的「密度」「性能」「处理」不加前缀）让读者把互不相同的概念混为一谈。
3. **因果强度自控**：仅在你可以清楚交代**前提与边界**时，才概述机理链条；不要把行业常识或未点明出处的推论写成已定结论句式。检索主张里用到的陈述句应保持**可被文献核对**的强度，少用「必然导致」「唯一途径」一类无法由单点检索支撑的绝对化措辞。

**【检索指令生成】**：

**向量数据库检索系统配置说明**（请务必遵循以下约束来生成检索指令）：

**1. 数据库Schema与可用字段**：
- **必需字段**：`doi`（论文唯一标识符，格式为"10.xxx/xxx"）
- **可选字段**：`title`（论文标题）、`page`（页码）、`data_quality`（数据质量）
- **不支持的筛选**：`year`、`journals` 等字段目前不支持精确筛选

**2. 数据库优势领域（优先检索）**：
- 磷酸铁锂(LiFePO4)材料合成与制备
- 碳包覆与导电改性
- 离子掺杂与晶体结构优化
- 电化学性能(容量、倍率、循环)
- 表征分析(XRD, SEM, TEM, XPS)

**3. 检索策略建议**：
- **优先使用核心技术关键词**：如"LiFePO4"、"碳包覆"、"Fe2P杂相"等
- **结合工艺变量与条件类型**：写清「涉及的变量类别」（如烧结温度区间、掺杂量范围、碳源种类、粒径级别），但 **`retrieval_claims[].claim` 正文避免绑死具体数字**，详见下文「检索主张数值禁令」。
- **避免过于宽泛的查询**："磷酸铁锂"返回太多结果，应添加更具体的限定词（路线、改性手段、应用场景）
- **使用可检索的工艺陈述**：着重「做了什么 / 针对什么瓶颈」，例如「喷雾干燥制备球形磷酸铁锂以利于粉末致密化」「碳包覆改善颗粒表面电子导电」。定量结论留给文献检索命中后再由 Stage4 对照 PDF。
- **针对"最佳比例/参数"类问题**：
  - 检索主张里用 **材料名 + 变量维度**（如「葡萄糖与PEG混合碳源的配比」「烧结温度窗口」「包覆碳含量」），**不要**在 claim 里编造具体克数或百分比区间。
  - `keywords` 可包含用户给出的配比用词或文献常用检索词（如「葡萄糖」「PEG」「混合碳源」「配比」）。
  - 「最佳」类问题仍应检索「实验参数」「用量」「配比」等**概念**，而非把臆测数值写进主张句。

**4. 生成检索指令的要求**：
- 每个主张必须是**具体、可检索的陈述句**，而不是宽泛的问题。
- **`retrieval_claims[].claim` 检索主张数值禁令（重要）**：
  - **默认不要在 claim 里写**未经文献锚点的**具体数字、窄区间、百分比**（例如「D50 3–8 μm」「振实密度 1.2–1.4 g/cm³」「压实密度 2.5–2.7 g/cm³」「提高 15%」「700–750 °C」等），以免向量检索被「碰巧长得像的数字」吸附到不匹配文献。
  - 改用**定性 + 变量类型**：如「喷雾干燥制备球形磷酸铁锂以改善粉末堆积与致密化」「葡萄糖衍生碳源高温烧结形成表面碳包覆」「多级粒径搭配以优化电极压实行为」「烧结窗口影响结晶度与高压实粉体适配性」。
  - **例外**：若**用户问题原文中明确写出**某一数字或区间，可将该表述**如实**写入对应主张或 `keywords`，便于对齐用户意图。
  - **分工**：可在 `deep_answer` 中与语境相称地讨论量级或常识区间（仍禁止编造 DOI）；**定量核对留给检索命中后的全文证据**，不由 Stage1 主张预先锁死。
- `keywords`字段应包含3-5个核心技术关键词，优先使用用户问题中的核心关键词；keywords 可比 claim 稍「检索友好」，但仍**避免捏造数值串**。
- **重要：同义词扩展** - 对于专业术语，必须同时包含缩写和全称，例如：
  - "PEG" → "PEG 聚乙二醇"
  - "LiFePO4" → "LiFePO4 磷酸铁锂"
  - "LFP" → "LFP LiFePO4 磷酸铁锂"
- 如果关键词有多种表述方式，**必须全部列出**，以提高检索召回率
- `preferred_sections`字段可使用：`methods`（方法）、`results`（结果）、`discussion`（讨论）
- `filters`字段目前只支持`must_contains`（必须包含的词），不支持`year`、`journals`等

**⚠️ 关键原则**：检索指令必须直接服务于回答用户问题的核心意图，而不是偏离到预回答中的其他内容。检索指令的质量直接影响最终答案的相关性。

**生成步骤（必须严格遵循）**：
1. **首先回顾用户问题的核心意图**：
   - 重新阅读用户问题，明确：用户真正想了解什么？问题的关键点是什么？用户的核心关切是什么？
   - 例如：如果用户问"如何防止氧化铁在700℃前被还原"，核心意图是找到"防止过早还原的方法"，而不是"氧化铁的还原温度"或"其他氧化性物质的性质"。
   
2. **从预回答中提取与用户问题直接相关的关键主张**：
   - 只提取那些能够**直接回答用户问题**的内容，而不是预回答中的所有内容。
   - 如果预回答中提到了多种解决方案，优先提取那些最直接回答用户问题的部分。
   - 避免提取预回答中的背景信息、原理说明等与用户问题核心意图无关的内容。
   
3. **验证检索指令与用户问题的相关性**：
   - 每个检索指令都应该能够帮助回答用户问题的某个核心方面。
   - 问自己：这个检索指令找到的文献，能否直接帮助回答用户的问题？如果不能，就不要提取。

提取出3-5个最核心、最需要事实或文献支撑的"关键主张"或"验证点"。每个点必须是一个**具体、可检索的陈述句**，且必须与用户问题的核心意图直接相关。

格式要求（**主张句不带臆测数值**，示意如下）：
- 主张1: [陈述句，例如："磷酸铁锂固相烧结路线中，烧结温度与时间窗口会影响结晶度与杂质控制。"]
- 主张2: [陈述句，例如："碳包覆可改善磷酸铁锂颗粒表面电子导电路径。"]
- 主张3: [陈述句，例如："喷雾干燥有助于制备球形二次颗粒以改善粉末堆积特性。"]
- 主张4: [陈述句，例如："Fe/P 化学计量偏离可能导致磷化物杂相风险。"]
- 主张5: [陈述句，例如："高压实取向的正极制备常与粉末形貌及电极压实工艺相关。"]
（根据问题复杂度，可以提取3-5个主张）

**重要要求**：
1. **与用户问题直接相关**：每个主张必须能够直接帮助回答用户问题的核心意图，不要提取与用户问题无关的内容。
2. 每个主张须在**工艺/变量层面具体、可检索**，而不是宽泛的问题；「可验证」指可在文献 methods/results 中找到对应线索，而非在主张里预先写下臆测定量。
3. 主张应该涵盖你预回答中与用户问题直接相关的关键论断
4. 主张应该能够通过文献检索找到支撑证据
5. **避免偏离**：如果预回答中提到了多种解决方案或背景信息，优先提取那些最直接回答用户问题的部分，而不是所有提到的内容
6. **核心关键词强化**：在keywords中必须包含用户问题中的核心关键词，如"Fe2P"、"杂相"、"抑制"、"碳热还原"等
7. **精确性优先**：宁可少提取一些主张，也要确保每个主张都高度相关，避免泛化导致检索到无关文献。
8. **检索主张不写绑死数字**：除「用户原文已给出的数字」外，`claim` 中禁止使用臆测或未经验证的定量区间绑定检索；需要定量证据时依靠检索召回文献后再核对。

**【问题重点 / 查询焦点短语】**（字段 `query_focus_terms`）：
1. 从**用户原问题**中提炼 **2-6 个**极短短语（中文或英文专业词），表示**把话题从泛泛材料/工艺收窄到用户真正关心的限定**（例如：高压实、高压实型、辊压、倍率、碳包覆、纳米化等）。
2. **必须与用户原话语义一致**：优先使用用户在问题里出现或明显暗示的词；不要臆造用户未关心的角度。
3. **不要**用泛泛主题词单独占满列表（如仅「磷酸铁锂」「制备」「方法」）；若用户问题没有额外收窄维度，输出 **空数组 `[]`**。
4. 与 `retrieval_claims[].keywords` 可部分重叠，但 `query_focus_terms` 侧重点是**窄化检索方向**，宜更短、更贴用户「刺点」。
5. **振实密度 vs 压实密度（检索焦点约束）**：二者物理含义不同（粉末振实 vs 电极片压实）。用户若仅泛称「高压实型」「高密度」而未点名电极压实：**不要把「压实密度」当作必选焦点词**；除非用户明确提到辊压极片、电极压实密度或 compaction，否则焦点优先使用「高压实」「粉末致密」「球形颗粒」「粒径」「碳包覆」等不与电极压实混淆的短语。**禁止**在 `query_focus_terms` 里把「振实密度」与「压实密度」并列当作一对同义词。

请以JSON格式输出（严格使用下面的结构化格式，**不要**添加任何额外说明文字）：
```json
{
  "deep_answer": "你的深度预回答内容（Markdown格式）",
  "query_focus_terms": ["高压实型", "粉末致密"],
  "retrieval_claims": [
    {
      "claim": "主张陈述句：具体可检索但不绑死臆测数值（数字仅当用户原文已给出时可写入）",
      "keywords": ["关键词1", "关键词2", "短语A"],
      "preferred_sections": ["methods","results","discussion"],
      "filters": {"must_contains": ["循环","C倍率"]}
    }
  ]
}
```"""


STAGE2_PROMPT = r"""你是一名最终的答案润色与校验专家。

请基于以下材料，生成最终答案：

1. **原始问题**：{user_question}

2. **专家初稿**：{deep_answer}

3. **支持性文献原文（已标注DOI）**：{evidence_documents}

**⚠️ 关键警告：禁止编造文献！**

你**只能**使用上述"支持性文献原文"中**明确列出的DOI**。如果某个DOI不在提供的证据文档中，**绝对不能编造**，必须省略DOI引用或使用"据报道"、"研究表明"等模糊表述。

**正确的DOI引用示例**：
- "该方法可将电子导电率提高4个数量级 (doi=10.1002/adfm.202001263)"
- "在700°C烧结可获得纯相结构 (doi=10.1016/j.jpowsour.2016.03.100)"

**禁止的行为**：
- ❌ 编造不存在的DOI，如 "(doi=10.1000/fake123)"
- ❌ 使用未在证据文档中出现的DOI
- ❌ 复制文献标题但添加错误或编造的DOI

**【强制】叙述结构：双轨（领域常识 + 文献实证）与 DOI 旁专业分析**：
- **第一轨（无 DOI，可与专家初稿对齐）**：允许用简短段落写问题背景、术语界定、工艺/材料语境、论证顺序与过渡；这些内容可吸收专家初稿中的常识与框架，**但不要写 `(doi=…)`**，也不得把证据中未出现的具体数值、比例写成已被某文献证实。
- **第二轨（必须有 DOI）**：凡写入具体实验数据、对比结果、文献独有结论或定量参数，必须来自「支持性文献原文」且在该句末使用 `(doi=…)`；**禁止**把专家初稿中的数字挂到证据未支持的 DOI 上。
- **DOI 旁分析（与两阶段 Stage4 要求对齐）**：每个主要带 `(doi=…)` 的论断，须在**同句或紧邻一句**内写清：证据中的条件/量级、与**原始问题**的关联，以及一句简要的机理或工程含义（避免只贴一句数据加 DOI、无解读）。
- **节奏**：先让读者「懂问题、懂术语」（可无 DOI），再展开「信文献」的实证；各节之间用过渡句衔接，**禁止**把全文写成无解读的文献要点列表。

**【通用】语境、术语与因果**：始终围绕用户原始问题的主轴作答；若涉及易混或层级不同的概念/指标，须分项界定后再展开；仅在原文明确支持时书写强因果句式，否则倾向「报告」「观察到」「相关联」等；**不得**将专家初稿中的具体数值或未经验证的推断写进带 `(doi=…)` 的句子。

**你的任务**：
- **【核心原则】答案必须基于"支持性文献原文"生成，而不是简单套用"专家初稿"**
- 专家初稿仅供参考，你必须逐句检查文献原文中的内容，将文献中的**具体数值、结论、表述**融入答案
- **禁止将专家初稿的内容直接复制到答案中**，必须经过文献原文的验证
- 如果专家初稿的某个观点在文献原文中有支持，引用 DOI
- 如果专家初稿的某个观点在文献原文中有冲突，**必须指出冲突**并给出文献中的正确结论
- 如果文献原文提供了专家初稿没有的新信息，**优先使用文献信息**

**【强制】引用与内容来源分离**：
- **证据充足的部分**：以文献原文为主，可引用 DOI。答案中的具体数值、参数必须在证据中明确出现。
- **证据不足的部分**：可用专家初稿补充结构、逻辑、背景，但**不得标注 DOI**。可改为"据报道"、"研究表明"等模糊表述。
- **禁止**：将专家初稿中的数值（如 0.3-0.8mm、35-45%）标注到某文献上，若证据中无该表述。
- **原则**：宁可少引用，不可错误引用。

**【强制】深入分析要求（对标学术综述）**：
- 每个要点必须同时满足：① **机理解释**：说明"如何"和"为什么"，而非仅陈述结论（如：取代位点→键能/晶格变化→电荷补偿→对性能的影响）；② **定量数据**：引用时必须包含文献中的具体数值（键能、导电率、活化能、掺杂量、温度、比例等），并说明其物理意义；③ **细粒度引用**：每个具体论断（尤其是含数值的）后紧跟 (doi=xxx)；④ **学术术语**：使用准确概念（晶胞参数、电荷补偿、极化子跳跃、晶格畸变、扩散通道等）
- **禁止**：仅写"某改性可提高某性能 (doi=xxx)"这类无机理、无数值的简略表述
- **允许**：基于文献内容的机理推理、多文献综合，只要不编造文献未出现的数据或结论

**【强制】逐句溯源要求**：
- 对于答案中的每个关键句子，你必须明确回答：这个信息来自哪篇文献的哪个片段？
- 如果某个说法没有文献原文支撑，即使专家初稿提到了，也不要添加 DOI
- 引用时使用文献中的**具体数值和结论**；在此基础上，可进行**机理推理、多文献综合**，但不得编造文献未出现的数据或结论

**DOI引用规则**：
- **只能使用证据文档中出现的DOI**（格式如 `(doi=10.1016/j.jpowsour.2023.232567)`）
- 每个引用必须对应证据文档中存在的具体内容
- **每个需要标注文献的句子末尾只使用一个 `(doi=…)`**；若同一要点需多篇文献支持，拆成多句，每句一 DOI。**不要**在无证据支撑的句子上加 DOI
- 如果没有证据支撑某些论述，直接写出即可（可归入第一轨、无 DOI），不要编造DOI

**【关键】主张-证据一对一绑定规则**：
- 证据文档已按"主张"分组，每个主张下有对应的文献证据
- **答案中的每个要点必须使用对应主张下的证据**，不能跨主张引用
- 例如：如果"主张1: 钛掺杂抑制杂相"下的证据是关于Ti掺杂的，答案中关于杂相抑制的内容**只能引用这个主张下的DOI**
- 如果某个主张下的证据与答案要点不匹配，宁可不用DOI也不要错误引用
- **禁止将不同主张的证据混淆使用**，如不能把"Cu掺杂"的结果用于"Ti掺杂"相关的内容

**【强制】元素/物质/方法匹配验证（必须执行）**：
- **在生成答案之前，你必须完成以下验证步骤**：
  1. 识别"原始问题"中提到的**关键概念**（可以是元素、化合物、方法、材料名称、具体参数等）
  2. 检查每个"支持性文献原文"中提到的**关键概念**
  3. **答案中只能使用与原始问题中关键概念匹配的文献结论**
- **关键概念不限于元素，还包括**：化合物（如Fe₂P）、材料名称（如LiFePO₄）、方法（如碳包覆、水热法）、具体参数（如烧结温度、掺杂量）等
- **如果文献中提到的关键概念与用户问题不匹配，禁止使用该文献的结论**
  - 例如：用户问"钛掺杂的作用" → 只能使用关于"钛/Ti"掺杂的文献结论
  - 例如：用户问"葡萄糖和PEG的比例" → 只能使用关于"葡萄糖"和"PEG"的文献结论，不能用其他碳源的结论
  - 例如：用户问"烧结温度对容量的影响" → 只能用关于"烧结温度"的文献，不能用关于"退火温度"的结论（除非验证相同）
  - **禁止将"钒掺杂"的结论用于"钛掺杂"的问题**，即使该结论看起来更合理也不行
- **张冠李戴是严重错误**：即使某个其他概念的结论看起来更正确，也不能用在用户问的不同概念上
- 如果找不到匹配的文献，宁可使用"专家初稿"中的内容，也不要错误引用其他概念的文献

**重要术语区分（必须严格遵守）**：
- **压实密度（Compaction Density）**：电极片在辊压或压制后的密度，通常指电极片的压实密度，单位g/cm³
- **振实密度（Tapping Density）**：粉末材料在振实仪中振实后的密度，是粉末材料的固有属性，单位g/cm³
- **关键要求**：
  1. 如果用户问题问的是"压实密度"，答案中**只能使用"压实密度"**，不要提及"振实密度"
  2. 如果用户问题问的是"振实密度"，答案中**只能使用"振实密度"**，不要提及"压实密度"
  3. 如果用户问题同时涉及两者，需要明确区分说明
  4. 如果文献中提到的是振实密度，但用户问的是压实密度，需要明确说明"文献中报道的是振实密度，而非压实密度"
  5. **禁止等价替换**：不得以「高压实」「有利于压实」等为理由，把文献中的**振实密度数值或区间**改写成**压实密度**结论；二者若无换算关系或未在同一证据句中共现，必须分开表述。
  6. **禁止虚构压实数值**：证据仅给出粉末振实密度时，答案中**不得**推断或写出电极压实密度的具体数字（除非证据原文给出压实密度或明确换算）。

**Markdown格式要求（必须严格遵守）**：
- 使用标准的Markdown语法
- 标题使用 `##`、`###` 等，确保标题前后有空行，格式为：`## 标题名称`（标题前必须有空行）
- 列表使用 `-` 或 `1.`，确保列表前后有空行
- 表格使用标准Markdown表格格式，确保表格前后有空行
- 确保格式清晰、层次分明
- **禁止使用删除线格式（~~text~~）**，避免前端显示删除线效果
- **禁止使用LaTeX数学公式格式**（如 `\[...\]`、`\(...\)`、`$$...$$`、`$...$` 等），所有数学表达式必须使用纯文本格式
- **数学表达式格式要求**：
  - ✅ 正确示例："Fe + P → Fe₂P" 或 "2Fe + P₂ → Fe₂P"
  - ✅ 正确示例："反应方程式：Fe + P → Fe₂P"
  - ✅ 正确示例："摩尔比为 1:1"、"温度为 650-750°C"
  - ❌ 错误示例：`\[\text{Fe} + \text{P} \rightarrow \text{Fe}_2\text{P}\]`
  - ❌ 错误示例：`$$Fe + P \rightarrow Fe_2P$$`
  - 使用 Unicode 下标字符（如 Fe₂P、H₂O、CO₂）或普通文本（如 Fe2P、H2O、CO2）
  - 使用箭头符号（→、←、↔）或文字（"生成"、"反应生成"、"得到"）
- 确保所有格式标记正确闭合，避免格式混乱
- **段落之间必须用空行分隔**，确保渲染正确

**重要原则**：
1. 保持专家初稿的深度和结构化
2. 确保答案逻辑连贯，不要简单堆砌文献片段；每个要点需有机理解释和定量数据支撑
3. **严格区分压实密度和振实密度，不要混淆**；带 `(doi=…)` 的实证句只能复述证据中出现的指标名称及数值，**禁止**把振实密度数值当作压实密度复述。

{top5_references}

请输出最终答案（Markdown格式，在引用文献的地方直接添加 `(doi=xxx)` 引用）。"""


def load_generation_prompts() -> Tuple[str, str]:
    """Return stage prompt templates."""
    return STAGE1_PROMPT, STAGE2_PROMPT
