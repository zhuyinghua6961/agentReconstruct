# Legacy public-service config location.
#
# Canonical public-service config now lives at:
# resource/config/services/public-service/config.shared.env
#
# This file is kept only so older scripts that reference it can continue to
# load without failing. Put new public-service behavior config in the resource
# service config file, shared infrastructure in resource/config/shared, and
# local secrets in untracked secret env files.
