# Mount Conventions

Use this directory to document mount-point conventions for:

- object storage mirrors
- external corpora disks
- model disks
- NFS or shared volumes

This directory is for conventions and placeholders, not for committed data.
